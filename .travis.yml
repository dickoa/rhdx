language: r
sudo: false
cache: packages
dist: trusty

after_success:
  - Rscript -e 'covr::codecov()'

addons:
  apt:
    packages:
      - libudunits2-dev
      - libgdal-dev

notifications:
  email:
    on_success: change
    on_failure: change
